# Team logo overrides

To use custom round "avatar" images for teams (like ESPN), add image files here and ensure they match the paths in `lib/teamLogos.ts`.

Default expected files:

- `bradys-bunch.png`
- `try-that-in-a-small-town.png`

Tip: square images (e.g. 512×512) work best.



